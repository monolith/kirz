<% title "Posts" %>

<table>
  <tr>
    <th>Description</th>
  </tr>
  <% for post in @posts %>
    <tr>
      <td><%=h post.description %></td>
      <td><%= link_to "Edit", edit_post_path(post) %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Post", new_post_path %></p>
