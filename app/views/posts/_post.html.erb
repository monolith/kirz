<div class= thumb id=thumb_<%= post.id.to_s %>><%= link_to image_tag(post.image.url(:small), :onload => "new Effect.Opacity('thumb_#{post.id.to_s}', { from: 0, to: 1, duration: 2 }); return false;"), post_path(post) %></div>

